@{
    Layout = "_Layout";
}

@model Factory.Models.Engineer

<h2>Engineer Details</h2>
<h3>@Html.DisplayNameFor(model => model.PersonName): @Html.DisplayFor(model => model.PersonName)</h3>

@if (@Model.EngineerMachines.Count == 0)
{
    <p>This machine does not belong to any engineers</p>
}
else
{
    <h4>engineers the machine belong to:</h4>
    <ul>
    @foreach (EngineerMachine join in Model.EngineerMachines)
        {
            <li>@join.Machine.MachineName</li>
            @using (Html.BeginForm("DeleteJoin", "Engineers"))
            {
                @Html.Hidden("joinId", @join.EngineerMachineId)
                <input type="submit" value="Delete"/>
            }
        }
    </ul>

}

<p>@Html.ActionLink("Back to list", "Index")</p>
<p>@Html.ActionLink("Assign a machine to this engineer", "AddMachine", new { id = Model.EngineerId })</p>
<p>@Html.ActionLink("Edit this Engineer", "Edit", new { id = Model.EngineerId })</p>
<p>@Html.ActionLink("Delete Engineer", "Delete", new { id = Model.EngineerId })</p>